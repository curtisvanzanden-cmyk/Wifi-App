# Legacy prototypes

These scripts are kept for reference only. Use the main application instead:

```bash
python main.py
```

| File | Description |
|------|-------------|
| `Tool.py` | Live WiFi signal monitor with matplotlib charts |
| `Tool0.01.py` | Basic floorplan click-to-measure heatmap |
| `tool0.02.py` | Monolithic predecessor to the current packaged app |
